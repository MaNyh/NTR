/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  2.1.x                                 |
|   \\  /    A nd           | Web:      www.OpenFOAM.org                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createPatchDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

// This application/dictionary controls:
// - optional: create new patches from boundary faces (either given as
//   a set of patches or as a faceSet)
// - always: order faces on coupled patches such that they are opposite. This
//   is done for all coupled faces, not just for any patches created.
// - optional: synchronise points on coupled patches.
// - always: remove zero-sized (non-coupled) patches (that were not added)

// 1. Create cyclic:
// - specify where the faces should come from
// - specify the type of cyclic. If a rotational specify the rotationAxis
//   and centre to make matching easier
// - always create both halves in one invocation with correct 'neighbourPatch'
//   setting.
// - optionally pointSync true to guarantee points to line up.

// 2. Correct incorrect cyclic:
// This will usually fail upon loading:
//  "face 0 area does not match neighbour 2 by 0.0100005%"
//  " -- possible face ordering problem."
// - in polyMesh/boundary file:
//      - loosen matchTolerance of all cyclics to get case to load
//      - or change patch type from 'cyclic' to 'patch'
//        and regenerate cyclic as above

// Do a synchronisation of coupled points after creation of any patches.
// Note: this does not work with points that are on multiple coupled patches
//       with transformations (i.e. cyclics).
pointSync false;

// Patches to create.
patches
(

// ************************************************************************* //
// ***************************CYCLICS CASCADE******************************* //
// ************************************************************************* //

    {
        // Name of new patch
        name PER1_1;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch PER2_1;

            transform translational;
            separationVector (0 0.076469997406006 0);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_per1_1);

    }
    {
        // Name of new patch
        name PER2_1;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch PER1_1;

            transform translational;
            separationVector (0 -0.076469997406006 0);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_per2_1);
    }
    
// ************************************************************************* //
    
    {
        // Name of new patch
        name PER1_2;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch PER2_2;

            transform translational;
            separationVector (0 0.076469997406006 0);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_per1_2);

    }
    {
        // Name of new patch
        name PER2_2;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch PER1_2;

            transform translational;
            separationVector (0 -0.076469997406006 0);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_per2_2);
    }
    
// ************************************************************************* //

    {
        // Name of new patch
        name PER1_3;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch PER2_3;

            transform translational;
            separationVector (0 0.076469997406006 0);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_per1_3);

    }
    {
        // Name of new patch
        name PER2_3;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch PER1_3;

            transform translational;
            separationVector (0 -0.076469997406006 0);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_per2_3);
    }
    
// ************************************************************************* //
// ***************************CYCLICS HUB/SHROUD**************************** //
// ************************************************************************* //


    {
        // Name of new patch
        name HS_PER1_1;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_1;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_1);

    }
    {
        // Name of new patch
        name HS_PER2_1;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_1;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_1);
    }

// ************************************************************************* //

    {
        // Name of new patch
        name HS_PER1_2;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_2;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_2);

    }
    {
        // Name of new patch
        name HS_PER2_2;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_2;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_2);
    }

// ************************************************************************* //

    {
        // Name of new patch
        name HS_PER1_3;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_3;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_3);

    }
    {
        // Name of new patch
        name HS_PER2_3;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_3;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_3);
    }

// ************************************************************************* //

    {
        // Name of new patch
        name HS_PER1_4;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_4;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_4);

    }
    {
        // Name of new patch
        name HS_PER2_4;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_4;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_4);
    }

// ************************************************************************* //

    {
        // Name of new patch
        name HS_PER1_5;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_5;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_5);

    }
    {
        // Name of new patch
        name HS_PER2_5;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_5;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_5);
    }

// ************************************************************************* //

    {
        // Name of new patch
        name HS_PER1_DS;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_DS;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_downstream_1_1);

    }
    {
        // Name of new patch
        name HS_PER2_DS;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_DS;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_downstream_1_1);
    }

// ************************************************************************* //

    {
        // Name of new patch
        name HS_PER1_US;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER2_US;

            transform translational;
            separationVector (0 0 0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_shroud_upstream_1_1);

    }
    {
        // Name of new patch
        name HS_PER2_US;

        // Dictionary to construct new patch from
        patchInfo
        {
            type cyclic;
            neighbourPatch HS_PER1_US;

            transform translational;
            separationVector (0 0 -0.06);
            matchTolerance 0.02;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_hub_upstream_1_1);
    }


// ************************************************************************* //
// ******************************RENAME PATCHES***************************** //
// ************************************************************************* //

    {
        // Name of new patch
        name INLET;

        // Dictionary to construct new patch from
        patchInfo
        {
            type patch;
 	        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_inflow_1);

    }
    {
        // Name of new patch
        name OUTLET;

        // Dictionary to construct new patch from
        patchInfo
        {
            type patch;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_outflow_1);
    }

    {
        // Name of new patch
        name BLADE_PS;

        // Dictionary to construct new patch from
        patchInfo
        {
            type patch;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_main_blade_2);

    }
    {
        // Name of new patch
        name BLADE_SS;

        // Dictionary to construct new patch from
        patchInfo
        {
            type patch;
        }

        // How to construct: either from 'patches' or 'set'
        constructFrom patches;

        // If constructFrom = patches : names of patches. Wildcards allowed.
        patches (row_1_main_blade_1);
    }
);

// ************************************************************************* //
